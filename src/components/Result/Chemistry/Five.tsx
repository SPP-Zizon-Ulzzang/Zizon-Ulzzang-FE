import { styled } from 'styled-components';

import { MemberData } from '../../../types/mbti';

interface FiveProps {
  memberData: MemberData[];
}
const Five = ({ memberData }: FiveProps) => {
  return <StFiveWrapper>미완성입니다</StFiveWrapper>;
};

export default Five;

const StFiveWrapper = styled.section``;
